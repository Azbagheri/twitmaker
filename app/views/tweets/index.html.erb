<ul class="tweets">
   <% @tweets.each do |twit| %>
    <%= render partial: 'single_tweet', locals: {tweet: twit}%>
   <% end %>
</ul>

<%= form_for @tweet do |f| %>
  <%= f.text_area :message, placeholder: "What did you learn today?" %>
  <%= f.submit "Tweet", id: "create-tweet", :data => { disable_with: false } %>
<% end %>
